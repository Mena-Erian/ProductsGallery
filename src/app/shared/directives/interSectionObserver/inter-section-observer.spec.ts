import { InterSectionObserver } from './inter-section-observer';

describe('InterSectionObserver', () => {
  it('should create an instance', () => {
    const directive = new InterSectionObserver();
    expect(directive).toBeTruthy();
  });
});
