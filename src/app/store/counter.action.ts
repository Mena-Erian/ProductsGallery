import { createAction } from '@ngrx/store';

export const increaseCounter = createAction('increaseCounter');

// export const decreaseCounter = createAction("decreaseCounter")
// export const resetCounter = createAction("resetCounter")
